{
  "name": "opencode-avatar",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:plugin": "pnpm --filter @opencode-avatar/plugin dev",
    "dev:app": "pnpm --filter @opencode-avatar/app dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
